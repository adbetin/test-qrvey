import { CountryPopulationPipe } from './country-population.pipe';

describe('CountryPopulationPipe', () => {
  it('create an instance', () => {
    const pipe = new CountryPopulationPipe();
    expect(pipe).toBeTruthy();
  });
});
