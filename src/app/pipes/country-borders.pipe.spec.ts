import { CountryBordersPipe } from './country-borders.pipe';

describe('CountryBordersPipe', () => {
  it('create an instance', () => {
    const pipe = new CountryBordersPipe();
    expect(pipe).toBeTruthy();
  });
});
